/**
 * The operator pages of the identity contract's group 5 by the route
 * segment that names each, the list the router registers routes from and
 * the page component draws by; kept apart from the components so the
 * router reads the names without loading the pages.
 */
export const IDENTITY_ADMIN_PAGES = [
  'dashboard',
  'users',
  'organizations',
  'logins',
  'registrations',
  'sessions',
  'service-usage',
  'insights',
  'client-health',
  'provider-health',
  'brute-force',
  'terms',
  'email-templates',
];

export const IDENTITY_RECORD_PAGES = ['user'];
