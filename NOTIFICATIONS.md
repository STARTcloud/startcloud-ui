# Gaps met by the startcloud-ui growth

- Branding, Accessibility, and identity decision 70: the computed on-primary rule names `#212529` for `moonshinedev` (`#1f9d57`), but `#1f9d57` against `#212529` is 4.42:1 and the same section's 4.5:1 gate refuses it (`#ffffff` is 3.49:1, so the rule's two choices both fail and the generator refuses the pack as the rule says); `switchboard` (`#24ade3`) against `#212529` is 5.99:1 and `nomadservices` (`#6c5ce7`) against `#ffffff` is 4.86:1, both as named. The committed `moonshinedev.yaml` carries `on_primary: '#000000'` (6.02:1) so the build passes; the contract needs to name the moonshinedev on-primary, or a third computed choice, that clears its own gate.
- Branding, Artwork, the files table, and identity decision 73: none of the files that table lists exists in this build yet, `public/brand/<site>/icon.png` and `logo-small.png` for the four sites, `public/brand/providers/<id>.svg`, the optional `public/themes/<pack>/mark.svg`, and `public/themes/switchboard/poppins-<weight>.woff2`; the shell ships its fallbacks (the build's own mark, the provider's name, Helvetica for the switchboard headline) until each is supplied, and `switchboard.yaml` gains its `fonts` entries when the Poppins files land.
