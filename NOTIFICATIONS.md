# Notifications from the STARTcloud UI estate

Every item lands here from another repository's `notifications.md`, is done or moved to the repository that owns it, and leaves.

## From authorization-server-private

| contract                                                                                                                                                                                                           | what is written                                                                                                                                                                                                                            | required change                                                                                                                                                                                                                                                                                                                                                        | source if absent                                                                                                                                                       |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| universal-identity.md, group 2 `region`, `regions_offered`, `?region=`, decisions 133 and 144; group 4 preferences `region`; universal-validation.md `preferences.region`; preferences-and-branding.md, Write path | detection first by GeoIP, a stored region beats every guess, the terms page offers a region selector whose choice is stored as `preferences.region`                                                                                        | draw the selector on TermsPage from `regions_offered`, re-read with `?region=`, write `PATCH /api/user/preferences { region }`, and accept `region` on the Preferences tab as a select                                                                                                                                                                                 | authorization-server-private: src/main/groovy/startcloud/integration/TermsOfServiceService.groovy, src/main/groovy/startcloud/user/UserPreferencesApiController.groovy |
| universal-identity.md, group 5, decisions 139 and 140                                                                                                                                                              | `POST /api/admin/organizations/bulk` and `DELETE /api/admin/brute-force` are served as the rows ask                                                                                                                                        | walk the All organizations bulk bar and the Blocked IPs Unblock all against them                                                                                                                                                                                                                                                                                       | authorization-server-private: src/main/groovy/startcloud/api/AdminApiController.groovy, src/main/groovy/startcloud/api/AdminOpsApiController.groovy                    |
| universal-identity.md, group 5 "What the admin pages send" and "What the admin pages draw" (Users), decision 146                                                                                                   | `POST /api/admin/users/bulk { action, user_ids, role, customer_id, primary_organization }`, `action` gains `set_customer_id`, `set_primary_organization`, `revoke_sessions`, `unlock`; every skipped row named in `errors: [{ id, code }]` | the Users action pane gains Set customer id (a small form dialog, empty clears), Set primary organization (a select over the organizations), Revoke sessions and Unlock; Revoke sessions behind a confirm and the step-up dialog like Delete; the result line naming processed, skipped and errors with `not_a_member`, `not_found` and `self` translated              | authorization-server-private: src/main/groovy/startcloud/api/AdminApiController.groovy                                                                                 |
| universal-identity.md, group 5 "What the admin pages send" and "What the admin pages draw" (Organizations), decision 147                                                                                           | `POST /api/admin/organizations/bulk { action, organization_ids, customer_id, access_mode, default_role }`, `action` gains `set_customer_id`, `set_access_mode`, `set_default_role`, `regenerate_invite_code`                               | the All organizations action pane gains Set customer id, Set access mode (a select over invite, request, private), Set default role (a select over MEMBER, ADMIN) and Regenerate invite code beside Suspend, Resume and Delete; a personal organization skipped with `personal`; the result line naming processed, skipped and errors                                  | authorization-server-private: src/main/groovy/startcloud/api/AdminApiController.groovy                                                                                 |
| universal-identity.md, group 5 "What the admin pages send" and "What the admin pages draw" (Activity, Sessions), decision 148                                                                                      | `POST /api/admin/sessions/bulk { action: "revoke", session_ids: [] }`, stepped up, `id` the surrogate the list carries                                                                                                                     | the Sessions table gains a select column whose header cell is a real checkbox, the select-all for the page, and while rows are picked the heading's action pane reads "N selected", Clear selection and Revoke, behind a confirm and the step-up dialog; the result line naming processed, skipped and errors                                                          | authorization-server-private: src/main/groovy/startcloud/api/AdminApiController.groovy                                                                                 |
| universal-identity.md, group 5 "What the admin pages send" and "What the admin pages draw" (Blocked IPs), decision 149                                                                                             | `POST /api/admin/brute-force/bulk { action: "unblock", addresses: [] }`, `id` the address, `not_blocked` for an address off the table, one `blocked-count` event after the selection                                                       | the Blocked IPs table gains a select column whose header cell is a real checkbox and, while rows are picked, "N selected", Clear selection and Unblock in the action pane beside Unblock all; the result line naming processed, skipped and errors                                                                                                                     | authorization-server-private: src/main/groovy/startcloud/api/AdminOpsApiController.groovy                                                                              |
| universal-identity.md, group 5 "What the admin pages send" and "What the admin pages draw" (Terms), decision 150                                                                                                   | `POST /api/admin/terms/bulk { action, ids: [] }`, `action` one of `delete`, `set_public`, `set_private`, `ids` the template ids, delete stepped up                                                                                         | each Terms card gains a checkbox and the heading a select-all checkbox; while cards are picked the action pane reads "N selected", Clear selection, Make public, Make private and Delete, Delete behind a confirm and the step-up dialog; the result line naming processed, skipped and errors                                                                         | authorization-server-private: src/main/groovy/startcloud/api/AdminOpsApiController.groovy                                                                              |
| universal-identity.md, group 4 "What the signed-in pages send" (inbox) and "What the signed-in pages draw" (InboxPage), decision 151                                                                               | `POST /api/notifications/{id}/unread` answering `204`, clearing `readAt`, the `unread-count` event following                                                                                                                               | the Inbox bulk bar gains Mark as unread beside Mark as read and Delete, one call per picked row; the result line naming processed, skipped and errors                                                                                                                                                                                                                  | authorization-server-private: src/main/groovy/startcloud/notification/NotificationController.groovy                                                                    |
| universal-identity.md, group 4 "What the signed-in pages send" (organizations) and "What the signed-in pages draw" (OrgConsolePage), decision 152                                                                  | `POST /api/user/organizations/{uuid}/invites/{id}/resend` answering `204`, `429` `throttled` with `wait_seconds` inside the sixty-second window, `404` `not_found`; the members and invitations bulk bars over the existing per-row routes | the console's Members tab gains a select column and, while rows are picked, Change role (a role select) and Remove; the Invitations tab a select column with Revoke and Resend, plus Resend per row; each sent as the per-row route once per picked row, the last-owner and managed-row refusals counted per row; the result line naming processed, skipped and errors | authorization-server-private: src/main/groovy/startcloud/api/UserOrganizationsApiController.groovy                                                                     |
| universal-identity.md, group 4 "What the signed-in pages draw" (ProfilePage, Favorites), decision 152                                                                                                              | no new route; `PUT /api/user/favorites` with the whole list                                                                                                                                                                                | the Favorites list gains a select column whose header cell is a real checkbox and, while rows are picked, "N selected", Clear selection and Remove, which writes the list without the picked rows                                                                                                                                                                      | authorization-server-private: src/main/groovy/startcloud/user/UserFavoritesApiController.groovy                                                                        |
