# Notifications from the STARTcloud UI estate

Every item lands here from another repository's `notifications.md`, is done or moved to the repository that owns it, and leaves.

## From BoxVault

Answers to the four rows this UI filed in `../BoxVault/notifications.md` on 2026-09-09; each row leaves when the UI has read it.

| row filed                                 | BoxVault's answer                                                                                                                                                                                                                                                                                                                                                                        |
| ----------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ISO pages with versions and architectures | already answered: `backend/app/routes/iso.routes.js` serves every path `src/features/collections/isos/api/isos.js` calls, the item and list answers carry `versions[] → files[]` with `architecture`, `fileName`, `fileSize`, `checksum`, `checksumType` and `downloadCount` per file, `/api/isos/discover` is widened by the box rule, and no single-file path remains; nothing changes |
| the `$defs` list and `nonBlank`           | done: `backend/app/rules/defs.json` carries the contract's `$defs` verbatim with `email` as the pattern, every required string form member carries `minLength: 1` and `pattern: "\\S"` (reported as `rule: pattern`, `params.pattern: nonBlank`, the UI's shape), the evaluator treats `required` as presence alone; the version form moves to the contract's `if`/`then` with it        |
| no server `message` rendered              | landing now: every failed read and refused write in `backend/app` answers the RFC 9457 body with a registry `type`, the translated text as `title`                                                                                                                                                                                                                                       |
| the pre-paint hash                        | nothing to change: BoxVault sends no Content-Security-Policy header at all (no helmet, no hash), so no `script-src` carries the old hash; the day BoxVault enforces a CSP it takes the hash from the release's `.prepaint.sha256`                                                                                                                                                        |

## From provisioner-catalog

The four rows filed in `../provisioner-catalog/notifications.md` are done there: every `params.pattern` the Worker emits is a contract `$defs` name (`nonBlank`, `watchId`), `required` means present only, every 400, 401, 403 and 404 the Worker answers is the RFC 9457 body with a registry `type`, the five push paths answer without `/api`, the catalog sends no Content-Security-Policy anywhere so no hash exists to move, and the three tooling files match this repository's origin copies. One contract gap remains: `universal-validation.md`, The error body, the `type` row, binds a `type` to 422, 409, 400, 403, 404, 500, 401 and 429 alone, while the Worker answers `405` (wrong method), `502` (a gateway fault: the store, Pages, the dispatch, the issuer's discovery or the hub not answering) and `503` (a missing configuration: push, dispatch or the hub not configured) as `{"error":"…"}` because no `type` exists for them; the registry gains a `type` for each, or names the body those statuses keep, and the Worker then routes the three through its problem helper.
