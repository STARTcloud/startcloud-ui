# startcloud-ui growth guide — the auth server as a role of the shared UI

Written by the auth-server session for the session that grows G:\Projects\startcloud-ui and for this one. Every statement is either read from a file (path given) or a decision Mark spoke in chat. Items marked UNVERIFIED were not read; read them before acting. Nothing here is a go; Mark gives go per change. This file is the tracking plan: a piece is done when its row in §3 says so.

## 0. Rules and pointers

- Mark's global CLAUDE.md applies: no comments in any file ever; read every file in full before any claim or edit; no shell commands, hand them to Mark in WSL Bash; no git commands; questions get answers only; explicit go per change; match Mark's existing patterns, never invent; five-line banner, two bars, 6-sentence cap, caveman full.
- The contracts now live in G:\Projects\startcloud-ui\docs\guides (moved 2026-09-06; the auth server keeps pointer stubs). Contract-first: a route, token, topic, provider or storage key lands in its contract before any code.
- The word "host" in the contracts was renamed "UI backend" (the application, in any language, that serves the build and answers GET /api/status).
- Visual references: universal-identity.html (five groups), universal-sidebar.html (the sidebar) and preferences-and-branding.html (the four sites), all beside the contracts in G:\Projects\startcloud-ui\docs\guides.
- The gap channel: when the startcloud-ui session meets a page, shape or key the contracts do not say, it writes the gap into G:\Projects\startcloud-ui\NOTIFICATIONS.md (the page, the shape it needs, the contract section it belongs to), and Mark carries it here; this session answers by amending the contract, never by a fix in either repo alone, and the other apps stay safe because the conformance checklists and the adapter and token gates decide what draws where.
- Settled for good, never re-raised: the password floor is 15 (NIST SP 800-63B rev 4 §3.1.1.2), published by `/api/rules`; the contract drives every shape, and where this server or another app differs from a contract the app changes, with a NOTIFICATIONS.md addendum in that repo when the app is not this one; this work is the UI conversion of the auth server and nothing else from priorities.yaml; the shared UI grows first, both run locally in WSL; the guide's work items are named in chat by what they are, never by row number, and "twins" is never said.

## 1. What the shared UI is today (read in full this session)

Repo G:\Projects\startcloud-ui, React 19, React Router 7, Vite 8, react-bootstrap 2.10, Bootstrap 5.3, i18next 26, axios, react-icons/fa6, loglevel, prop-types, react-markdown, react-country-flag.

Tree (src/): app/{index.jsx, App.jsx, provider.jsx, router.jsx, callback.jsx}, components/{common, layout}, contexts/{StatusContext, NoticeContext, SearchContext}, hooks/{useSession, useTheme, useFavicon, useSetupGate, useAccountAvatar, useAccountPreferences, useSessionKeepalive, useEventStream, useActiveOrganization, useSearchBinding, useFormRules, useTicketUrl}, lib/{runtime, createSession, apiClient, browserOidc, backendSession, anonymousSession, dpop, jwt, returnTo, events, eventHub, sse, i18n, logger}, utils/{capabilities, routes, validation, ticketUrl, relativeTime, identity, membership, auth, responseMessage, formatFileSize, gravatar, organizations}, config/{brand.js, constants.js}, css/{styles.css, fonts.css}, features/{auth, admin, organizations, profile, setup, catalog, collections/{registry, boxes, isos, provisioners}, deploy, about, notifications, search, vdi}. Locales public/locales/{en,es,cimode}/{shared.json, auth.json}. Two HTML entries: index.html and callback/index.html.

Boot (src/app/index.jsx): probeStatus (`GET /api/status` on the serving origin) → initRuntime(status) → configureLogger → createI18n → Promise.all([i18n ready, loadRules() = `GET /api/rules`, null on 404]) → render.

Shell (src/components/layout/AppShell.jsx): a viewport-high column of Header (brand, crumbs or utility links, the cluster), NoticeCards, the one scroll region with ErrorBoundary, Footer; reserved first segments `about, organizations, login, auth, register, invite, profile, admin, org-console, setup, callback, docs, schema, private, push, search, vm` plus collection segments; no sidebar today.

Session (src/lib/createSession.js, src/utils/capabilities.js `authMethod`): first `auth` token; `[]` → anonymousSession, `idp` → browserOidc, else backendSession. Provider interface per useSession: `id`, `issuerUrl`, `restore()`, `load()`, `reload()`, `refresh()`, `begin(opts)`, `complete()`, `headers(method, url)`, `retryAuth()`, `adoptResponse(headers)`, `endSession()`, `claims()`, `savePreferences(patch)`, `signOut()`, `signOutEverywhere()`; backendSession adds `login(username, password, stayLoggedIn)`, `current()`, `authHeader()`.

Routes (src/app/router.jsx): `/setup`, `/`, `/vm/:instance`, `/about`, `/search`, `/organizations/discover`, `/login`, `/auth/callback`, `/register`, `/invite/:token`, `/profile`, `/admin`, `/org-console`, the collection routes, `*` → `/`; a failed gate renders NotAvailableStub.

Admin (src/features/admin): AdminPage with Organizations and users (adapter `organizationsWithUsers`), Configuration (`status.config`, AdminConfig over the served schema), System (`storage`); UpdateNotice from `status.role`.

Error handling: src/components/common/ErrorBoundary.jsx (card, refresh, home, stack in dev, `onError` → src/lib/logger.js `reportRenderError` → `/api/client-errors` on a backend UI backend).

Theme (src/hooks/useTheme.js, index.html pre-paint): stamps `data-bs-theme` only; no `data-brand`, no pack stylesheet.

Dev proxy (vite.config.js, config.yaml): `/api/user/preferences` and `/api/notifications` → `auth_target` (`https://dev-auth.startcloud.com`), everything else → `api_target`.

CI: ci.yml (lint, prettier, locale parity, build, CodeQL), prod-build.yml (dist tarball), release-please.yml (notify-consumers: Makr91/BoxVault, STARTcloud/provisioner-catalog, STARTcloud/VDI-CIFS-Health-private; the auth server repo is not listed).

## 2. Decisions (a number is the identity draft's, a–g the sidebar's, h–m branding and build; §5 lists what is still open)

Shape:

- The auth server serves no HTML: every page is a page of the shared build served at `/`, role `auth-server`, first auth token `cookie`, a fourth session provider `createCookieSession`; restore caches the profile in localStorage under `account`, `load()` confirms through `GET /api/user`.
- GET on a shared path is the SPA page, POST is the server action; protocol redirects stay top-level navigations; a POST answers `{ next }` or `application/problem+json` with `code` (and `wait_seconds` when throttled) on `Accept: application/json`.
- Onboarding gate moves from the MVC interceptor to `403 onboarding_required` with `next` on `/api/*`; the authorize filter is unchanged; one state call `GET /api/auth/onboarding`.
- Feature tokens, the fourteen of decision y and the identity contract's status table: `local-accounts`, `tfa`, `onboarding`, `interstitials`, `policies`, `org-console`, `invitations`, `integrations`, `inbox`, `admin`, `notifications`, `health`, `events`, `footer` (12: `interstitials` over `oauth-interstitials`); the config editor is the shared editor, so no `issuer-config` token; passkeys, the magic link and the providers are rows of `GET /api/auth/methods`, not tokens, and the profile's Sessions, Favorites and Service accounts draw when the adapter carries their calls, so none of those is a token either.
- `/api/admin/**` gains the session-plus-CSRF principal beside Bearer.
- The fifty-odd XHR routes move under `/api/user/*`, JSON snake_case, problem bodies; browser paths unchanged (14, one release, no aliases).
- `POST /api/user/step-up { password | code }` arms a five-minute window and replaces `stepupPassword` and `stepupCode`; a sensitive call outside the window answers `403 step_up_required` and the dialog arms it and retries; no DELETE carries a body.
- Password minimum 15 from `/api/rules`; the confirmation field is the page's `equals` rule (group 2).
- The navbar is the shared chrome branded per site (`help_url` → `links.docs`, `support_email` → `links.contact`).

Identity draft decisions 1–20:
1 the shared LoginPage grows by mode and state · 2 the problem `code` vocabulary as drafted, `429` for the throttled case · 3 `403 reset_invalid` · 4 whatever does not annoy users; the Sign in button hidden on the issuer's auth pages · 5 the auth column's tokens map onto the pack variables, fonts per pack · 6 terms as server-rendered HTML the page injects · 7 `intl-tel-input` through its official React component, no invention · 8 `/oauth/terms` and `/oauth/privacy` retired for the site templates at `/public/policies/{name}`; the URLs registered with Google's verification get updated once (footnote, Google auth is not in active use) · 9 retire both: the verifier found `CompleteRegistrationController` reached by nothing but its own `permitAll` entry and self-redirects, and `completeRegistration.html` rendered only as the error re-render of `POST /registration/verify`, a closed loop since `GET /registration/verify` routes to `/complete-onboarding` · 10 the EFF wordlist bundled, loaded on demand · 11 `frame-src` set on the `index.html` answer for `/connect/logout/frontchannel` · 12 `interstitials` · 13 `/user/organizations` as a card list opening the shared console · 14 one release, no aliases · 15 Google Places kept behind the issuer's key · 16 the shared config editor is the target and the last piece, its own session; until it lands the Thymeleaf `/admin/config` page stays as the one named exception, no stopgap built · 17 admin pages as routes under `/admin/*`, every entry and tab a deep link · 18 the error page keeps the stack trace for admins through a one-hour store and `GET /api/admin/errors/{reference}`, the ticket description carries it, the error boundary ships render crashes with their component stack to `/api/client-errors` · 19 paged admin lists answer `{ items, page, size, total, total_pages }` · 20 the rate-limit admin routes get a Rate limits dialog on each Users row.

Sidebar decisions a–g:
a the manner is hyperweaver's: a full-height column on the left, its top the brand mark plus product name that collapses to a 38px rail, the header, page and footer to its right and moving with it; the window never scrolls, the entries and the page scroll on their own, the footer is the one fixed row · b entries come only from feature exports `sidebar(status, account)`, two kinds, sections and tree; a tree may carry views (Proxmox's manner); no entries from `/api/status` for now, an ad-hoc source may come later · c the sidebar shows on every page once a feature exported entries, collapsed or not per `sidebar_minimized` · d the foot is bare; page actions stay on the page · e the swap: while a feature exports `actionMenu` the header's account slot draws it and the user menu moves to the sidebar foot; theme and language stay in the header; hyperweaver would be the first fit, but hyperweaver is not being changed now · f the auth server's entries: for every signed-in person, from the profile feature, Account: Profile, Organizations, Integrations, Inbox; for an admin, from the identity feature's own operator export below it, Overview: Dashboard; Accounts: Users, All organizations; Activity: Logins, Registrations, Sessions; Health: Service usage, Insights, Client health; Security: Blocked IPs; Content: Terms; System: Configuration; the user menu carries no Admin and no Preferences row on the issuer, a row lives in the column or the menu, never both · g the shared AdminPage's three tabs become sidebar entries at `/admin`, `/admin/config`, `/admin/system` on every `admin` UI backend.

Branding and build decisions h–m (2026-09-06):
h a pack is a package of files a developer writes, the stylesheet, artwork and font files, one directory per pack under startcloud-ui's `public/themes/<pack>/`, shipped with the build and served by the origin that serves it; the auth server holds no pack and renders none, it names one per site through `theme_id` and answers `theme_css` as that stylesheet's URL · i the site default reaches the first paint without a fetch: Spring inserts one attribute, `data-brand-theme="<light|dark>"`, into the `<html>` tag of the built `index.html` as it serves it, by hostname; the file on disk never changes · j packs may set the `--bs-*` colour set, surfaces included, per variant, so a dev themes a site as they want; geometry is never a pack's, layout belongs to the feature · k `--brand-auth-display` is the one face a pack carries (decision 5), the chrome's faces stay the build's · l the UI dist is never fetched by Gradle or by the jar and never enters the jar: the pin lives in `packaging/config/ui-version.yaml` beside `version.yaml`, CI curls and untars `startcloud-ui-<pin>.tar.gz` into `ui/` in the checkout in dev-build, prod-build and ci, and the deb carries that folder as `/opt/prominic/authorization-server/ui/` beside `authorization-server.jar`, the shape of BoxVault's `backend/ui` and VDI Health's `/opt/vdi-health/ui`; a `dependency-bump.yml` here rewrites that file and opens the `bump/startcloud-ui` PR, and startcloud-ui's release-please consumers matrix gains `STARTcloud/authorization-server-private` · m the shared UI is served from the on-disk `ui/` folder at `/`, `index.html` the fallback after every API and protocol route, so packs and pages are files editable on disk, live on the next reload; the site's default variant is `sites.<id>.ui.default_theme`.

Status and stream decisions n–x (2026-09-06):
n `links.docs` and `links.contact` keep their site keys `assets.help_url` and `mail.support_email`; unset stays empty · o `ticket.fallbackCustomerId` is the site's `customer_id`: when the person's own is unknown, the app's customer id carries the improvement request · p `version` in `/api/status` is Spring's `build-info.properties` read through the `BuildProperties` bean at runtime, the manifest read the way BoxVault reads its `package.json` · q the events stream is the events contract's: `GET /api/events`, the `ready` frame, `session-terminated` and `unread-count` by name, resumable ids; `/api/notifications/stream` with its `connected` and `notification` events goes with `notifications.js` · r notification rows are the contract's: `readAt` in place of `isRead`, `navigate` an `https://` URL; the hub's answer changes here, and BoxVault's proxy of it gets a NOTIFICATIONS.md addendum · s the branding endpoint answers the contract's shape: `theme_css` returned, absolute URLs, sized logo objects, `theme_pack: null` for a light site · t no theme file of any kind lives in this repository, an API-only server; the packs are startcloud-ui's per decision h, and the site's `theme_id` only names one · u the served `index.html` carries the site default as `data-brand-theme` on `<html>` · v every string the identity pages draw is a key in startcloud-ui's `shared.json` and `auth.json`, written in `en`, `es` and `cimode` together by the UI work, the estate's existing i18n system, and this server's `messages.properties` keys retire with their templates · w the `search` feature stays out of this conversion · x dev setup is startcloud-ui's `config.yaml` with `api_target` and `auth_target` both the local server · y the issuer's feature words are exactly the gates the contracts name, no more and no fewer: `local-accounts`, `tfa`, `onboarding`, `interstitials`, `policies`, `org-console`, `invitations`, `integrations`, `inbox`, `admin`, `notifications`, `health`, `events`, and `footer` where the site wants it; a new word appears only when a contract adds the surface it gates, so the list grows with the contracts and is never padded · z `brand.repo` is omitted on the issuer and the footer's left slot is plain text, as the contract allows · aa the footer is a per-site switch: `sites.<id>.ui.footer: true|false` decides whether that site's `/api/status` lists `footer`, all of that site's pages alike; the six `show_footer_on_*` page flags go · ab no hand-written smoke tests in workflows: the two curl-script steps in ci.yml's login-smoke job are removed, a check that must survive becomes a proper tool in a reusable workflow, a browser test tool (Playwright if chosen) is a normal step of the standard ci.yml, the OIDC conformance suite under `packaging/tests` being the tool this repo already has; a Universal Testing Contract is written after the UI changes land and is listed in priorities.yaml · ad `/` on the issuer is the profile for a signed-in person, as `UserController.profile` already maps `''`, `/`, `index` and `/user/profile`, and `/login` for an anonymous visitor; a member without `can_manage` sees an organization's record and members read-only, the flags the template gates on today · ac the retirements (the templates, `auth.css`, the vendored Bootstrap library under `static/assets/libs`, jQuery, the `intl-tel-input` assets, `sidebarmenu.js`, `userDropdown.js`, the static `oauth/terms.html` and `oauth/privacy.html`, `completeRegistration.html`, `CompleteRegistrationController` and its `permitAll` entry) happen after the cutover, in one discussion.

Decisions 21 to 26 are the identity contract's own list; decisions 27 onward (2026-09-07) came out of the four review rounds (nonsense, spec, security, UX) and are written in the contracts, not here: every POST CSRF-checked and the cookie attributes fixed; `401` only for no session, `403` with a code on a live one; step-up on every change to how an account is entered or ended; one second-factor gate for every code, exponential and never a fixed lock; registration answers `202` for a taken address; JSON exports; `ROLE_ONBOARDING` for the pending account; emailed tokens consumed by a POST from an SPA route; no bodies on DELETE, `POST /api/user/step-up` arming a short window instead; `allow-credentials: false` with the sibling origins listed; `swb://` and `hwa://` kept as custom-scheme hand-offs; the jar serves no `/assets`, the issuer's images live in the build's `public/brand/` and the pack directories; the operator pages are the identity feature's own export, mounted by the `cookie` token, never a role branch in the shared admin feature; `navigate` may be `https://` or a same-origin path; `health` and `footer` together draw the heart; the sidebar rows "Inbox" and "All organizations", the menu without Admin and Preferences on the issuer, the app section headed by `brand.name`; a site that requires a phone says so with the support contact; Keep me logged in in both sign-in modes; the reset keeps sending the person to sign in (OWASP); admin filters on the page; sidebar rows only, no tab strip; the status payload's camelCase named as the one exception; the config page stays the named exception until the shared editor; restart-required, the blocked count and health travel on the `admin` events topic, never a timer; the language control is a globe with the current code and rows of code, own-language name and decorative flags; packs same-origin unless `sites.<id>.ui.pack_origins` lists the host.

Gaps closed:

- `status.analytics: { script_url, attribute, value }` keeps the Plausible or Umami tag.
- `footer` feature token; absent hides the footer; every existing backend adds the word.
- The issuer's `/api/status` per site by hostname; `brand.theme` for the site default variant before paint; `GET /api/health` in the contract shape; the pre-paint script hashed into `script-src`; the push worker served from the build at the origin root; `session-terminated` on the events stream.

## 3. Work items, in order (a row is done when marked)

| #   | Item                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Where                                                                                  | State                                                                                                                                                                                                                                                                                                                                                                                                                                                      |
| --- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | Contracts: sidebar section, `footer` token, storage keys, AdminPage entries                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         | startcloud-ui/docs/guides navbar, session, pages                                       | done 2026-09-06                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2   | Contracts: token table (`cookie`, `auth-server`, the issuer's features), session provider column, storage row, reserved segments, `status.analytics`, `brand.theme`                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | the same three plus preferences-and-branding                                           | done 2026-09-06: navbar and pages verified; session gains the cookie provider in the intro, principles, `createSession`, the shared list and the checklist column; preferences-and-branding gains `brand.theme`                                                                                                                                                                                                                                            |
| 3   | Identity contract groups 1–5 beside the contracts; group 5 redrawn around the sidebar; error frame with the details fold and the boundary case                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | startcloud-ui/docs/guides                                                              | done 2026-09-07: group 5 rewritten around the seven sidebar sections with the identity feature's operator export, the rows the only navigation and no tab strip, `admin` gate on `/admin/config`, ErrorPage answered in place with the admin fold and the boundary case; group 3 token `interstitials`; every ruling of the four review rounds written into the contracts as decisions 27 to 64; identity.html and the four other mockups redrawn to match |
| 4   | Sidebar mock with the tree view switch                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | startcloud-ui/docs/guides universal-sidebar.html                                       | done 2026-09-06: foot and Profile callouts aligned with the swap, `sidebar_view_<group>` in the contracts table; navbar.md Export gains `views`, Storage gains `sidebar_view_<group>`                                                                                                                                                                                                                                                                      |
| 5   | Brand frame: the four sites on the chrome from `auth.css` and the sites block                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | startcloud-ui/docs/guides preferences-and-branding.html, the branding contract's packs | done 2026-09-06: the live frame per site and variant, the four at rest, the status and branding answers, the generated pack, the sites table; the contract names the reference, `--brand-auth-display` joins the variable contract per decision 5                                                                                                                                                                                                          |
| 6   | UI growth, in the seven steps of §3a below                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | startcloud-ui                                                                          | done 2026-09-07: the cookie provider and the issuer's status; the sidebar and the exports; the packs and the generator; the five page groups; the error page; the event stream; the dev proxy; a development mock of the issuer under scripts/ until the server answers                                                                                                                                                                                    |
| 7   | Server endpoints, in this order: `GET /api/status` per site, `GET /api/health`, `GET /api/rules`, `GET /api/user`, the JSON answer on `POST /login`, `GET /api/auth/methods`, `GET /api/auth/tfa`, `GET /api/auth/tfa/methods`, the JSON answers on `POST /authenticator` and `POST /authenticator-method`, then the rest of `/api/auth/*`, `/api/user/*`, `/api/admin/*`, `/api/policies/*`, `/api/client-errors`, `/api/admin/errors/{reference}`, the remaining JSON answers on the shared POST paths, the error dispatch answering `index.html` in place with the fault's status and the stamped attributes, the session principal on `/api/admin/**`, the events stream with the `admin` topic, the branding endpoint's target shape with `theme_css` pointing at the pack in the served build | this repo                                                                              | to do, this session                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 8   | Build and serving per decisions l and m: `packaging/config/ui-version.yaml`, the CI fetch step into `ui/` in dev-build.yml, prod-build.yml and ci.yml, the deb's `opt/prominic/authorization-server/ui/` folder, `dependency-bump.yml`, the consumers line handed to startcloud-ui, the Spring resource handler for the on-disk `ui/` folder with the `index.html` fallback and the per-site `data-brand-theme` attribute, `sites.<id>.ui.default_theme`, `sites.<id>.ui.footer` and `sites.<id>.ui.pack_origins` in application.yml; the Vite proxy for the non-`/api` paths; ci.yml's two login-smoke curl steps removed per decision ab; this row lands before row 7, because `GET /api/status` per site reads the `ui` keys it adds                                                             | both                                                                                   | to do, before row 7                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 9   | Config editor: the shared editor over JSON Schema, the Java side answering nested plain values per section                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | both, its own session                                                                  | last                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 10  | Retirements once 1–8 land: the 61 templates, `auth.css`, the vendored Bootstrap, jQuery, `intl-tel-input` assets, `sidebarmenu.js`, `userDropdown.js`, the static legal pages, `completeRegistration.html`, `CompleteRegistrationController` and its `permitAll` entry                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | this repo                                                                              | after the cutover, discussion first                                                                                                                                                                                                                                                                                                                                                                                                                        |

## 3a. The UI growth in order

Each step names the contract sections it is built from, the files it touches, the locale work and the check that closes it; a step starts when the one before it is closed, and the local authorization server answers only what the server work has landed, so a step mocks the rest from the contract's example payloads until then.

| Step                                                                | Built from                                                                                                                                                                    | Files                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Locale                                                                                                                                                       | Closed when                                                                                                                                                                                                                     |
| ------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1 · the cookie session and the issuer's status                      | identity: The cookie session provider, The issuer's status payload; session: the provider table and storage table; navbar: the status payload and token table                 | `src/lib/cookieSession.js` new, the shape of `backendSession.js`; `src/lib/createSession.js` and `src/utils/capabilities.js` choose it on the first `auth` token `cookie`; `src/lib/runtime.js` appends `status.analytics` on every page as answered; `login_method` is LoginPage's own key and touches no hook                                                                                                                                                                                                                            | none                                                                                                                                                         | the shell boots against the issuer's `/api/status`, `restore()` reads `account`, `load()` answers the profile or `null` on `401`, `headers()` echoes `XSRF-TOKEN`, and the session checklist's cookie column is true            |
| 2 · the sidebar, the Account and operator exports, the footer token | navbar: Sidebar (Export with `views`, Storage), the `footer` and `health` tokens; pages: Sidebar entries; identity group 5: The sidebar export                                | `src/components/layout/Sidebar.jsx` new, `AppShell.jsx` for the grid, the crumbs and the hidden column on auth routes, `Footer.jsx` for the two tokens and the stream-fed heart; `src/features/profile/sidebar.js` (the Account section) and the identity feature's operator export new; the shared admin feature's entries at `/admin`, `/admin/config` and `/admin/system`, each drawn only while the adapter carries `organizationsWithUsers`, `config` or `storage`                                                                    | `account.sidebar.*` (`inbox` for the notifications row), `admin.sidebar.*` (`overview` included, `organizations.all`) in `shared.json`, `en`, `es`, `cimode` | universal-sidebar.html matches the shell on the issuer for a member and for an admin, no page draws a tab strip beside its rows, BoxVault's admin tab strip is gone, and a `backend` UI backend without exports draws no column |
| 3 · branding                                                        | preferences-and-branding: Rendering, The pre-paint script, Packs, Source format and generation, Pack names, Accessibility, Typography under Out of scope; identity decision 5 | `index.html` pre-paint reads `data-brand-theme`; `src/hooks/useTheme.js` the same chain; `src/css/fonts.css` with `font-display: swap` and the two auth faces preloaded; the pack generator (YAML in, CSS and the content hash out, the 4.5:1 and 3:1 gates refusing a failing pack) and its three YAML sources under `public/themes/moonshinedev/`, `switchboard/`, `nomadservices/` from the values in preferences-and-branding.html; the issuer's site marks and provider icons under `public/brand/` since the jar serves no `/assets` | none                                                                                                                                                         | the four sites of preferences-and-branding.html paint with the first frame on the served `index.html`, no repaint, the STARTcloud site carries no `data-brand`, and a pack failing contrast does not build                      |
| 4 · the five page groups                                            | identity groups 1 to 5, each group's routes, answers, draws, shared components and keys tables; validation for every field                                                    | `src/features/auth/` grown, `tfa/`, `onboarding/`, `interstitials/`, `integrations/` new, `profile/`, `organizations/`, `notifications/` (the InboxPage beside the modal's `InboxList`), `admin/` grown; the shared components each group's table names under `src/components/common/`; `src/lib/passkeys.js`, `wordlist.js`, `countries.js`; the routes in `src/app/router.jsx` behind the group's gate                                                                                                                                   | every key each group's keys section lists, `auth.json` and `shared.json`, in the three locales together, parity in CI                                        | every frame of universal-identity.html renders from the contract's example payloads, every reserved segment of the pages contract is in `AppShell`, and no string on any page is literal                                        |
| 5 · the error page and the boundary                                 | identity group 5: ErrorPage, The server's `/error` dispatch; navbar: the error boundary                                                                                       | `src/features/errors/ErrorPage.jsx` new at `/error` and for an unknown route; `src/components/common/ErrorBoundary.jsx` shipping to `/api/client-errors`                                                                                                                                                                                                                                                                                                                                                                                   | `errors.*` of the admin keys section                                                                                                                         | the error frame of universal-identity.html renders for a visitor, a member and an admin, and a render crash draws the boundary card                                                                                             |
| 6 · the event stream                                                | events: the auth server row, the `admin` topic and the checklist; identity: `events` in the status table, the sidebar badges and the footer heart                             | `src/hooks/useEventStream.js` over `status.events`, the `notifications`, `session` and `admin` topics, `session-terminated` ending the session on the bus, `unread-count` on the bell and the Inbox badge, `blocked-count` on the Blocked IPs badge, `restart-required` on the Dashboard and Configuration cards, `health` on the footer heart                                                                                                                                                                                             | none                                                                                                                                                         | one connection per tab on the issuer, every badge, card and heart updates without a poll, no timer exists in the shell, and a revoked session ends in every tab                                                                 |
| 7 · the dev proxy                                                   | guide decision x; navbar: Where the UI is served                                                                                                                              | `config.yaml` with `api_target` and `auth_target` both the local server; `vite.config.js` proxying every non-page path to it                                                                                                                                                                                                                                                                                                                                                                                                               | none                                                                                                                                                         | the shared UI on its dev server signs in against the local issuer through the proxy, end to end                                                                                                                                 |

## 4. Server endpoints the UI will need, listed for mocking

- `GET /api/status` per site, `GET /api/health`, `GET /api/rules`.
- `GET /api/user`, `PATCH /api/user`, the `/api/user/*` routes of identity group 4.
- `GET /api/auth/methods`, `/tfa`, `/tfa/methods`, `/tfa/enrol`, `/tfa/backup-codes`, `/onboarding`, `/terms`, `/consent`, `/ciba`, `/logout/confirm`, `/logout/frontchannel`, `/link`; `GET /api/policies/{name}`.
- JSON answers on `POST /login`, `/authenticator`, `/authenticator-method`, `/resend-tfa`, `/passwordRecovery`, `/passwordReset`, `/registration`, the `/complete-onboarding/*` steps, `/qrcode/verify`, `/oauth2/accept-terms`, `/ciba/approve|deny`, `/connect/logout/confirm`, `/link-account/confirm`.
- `/api/admin/*` of identity group 5 with the session principal; `GET /api/admin/errors/{reference}`; `POST /api/client-errors`.
- The error dispatch: `index.html` in place with the fault's status and `data-error-status`, `data-error-reference` and `data-error-path` on `<html>` for a page GET, a `303` to `/error?status&reference&path` after a failed POST alone, the problem body with `reference` on a JSON call; `CookieTheftException` → `/login?error=session_reset`.
- The events stream `{ path: "/api/events", topics: ["notifications", "session", "admin"] }` per decision q, the `admin` topic carrying `restart-required`, `blocked-count` and `health`.

## 5. Open

- Nothing; every decision through 99, a–g and h–ad is made and written into the contracts, the decisions 27 to 99 in the identity contract's own list, 65 to 99 answering the gaps of startcloud-ui's NOTIFICATIONS.md (2026-09-07); the Poppins files of decision 73 are Mark's to supply, the marks and provider icons landed. The UI work may begin, the shared UI first (decision in §0).

## 6. Handoff (state as of 2026-09-07; the planning is complete)

State of every artefact, so the next session reads before it acts:

- Every contract in G:\Projects\startcloud-ui\docs\guides is final for this conversion: universal-navbar, universal-session, universal-pages, universal-events, universal-validation, universal-config, preferences-and-branding and universal-identity, the last carrying decisions 1 to 99. Every ruling of the four review rounds (nonsense, spec, security, UX) is written into them as a rule with its reason; nothing is left in chat alone. Read every file in full before touching it.
- The five mockups beside them match the contracts: universal-identity.html (five groups, the signed-in chrome, the admin pages, the errors), universal-sidebar.html, universal-navbar.html, universal-pages.html and preferences-and-branding.html, each viewed as file:///G:/Projects/startcloud-ui/docs/guides/<file>. Where a mockup and a contract disagree the contract wins and the mockup is corrected.
- Auth-server stubs in its docs/guides point at the moved files with the GitHub URL and the local path.
- Next UI work is the UI growth of §3a, step 1 first, each step closed by its own check; next server work is the build and serving item and then the server endpoints in their stated order; the config editor is last. Never say "twins": name the endpoint.

The brief for the startcloud-ui session, in one paragraph: read this guide in full, then every contract and mockup named above in full, then start §3a at step 1 and stop at each step's "closed when"; every route, token, topic, storage key, component and locale key you write already has a line in a contract, and when a page, shape or key you need is not in one, write the gap into G:\Projects\startcloud-ui\NOTIFICATIONS.md as §0 says and carry on with the next thing that is, because the contract is amended from the auth-server side and never by a local invention; the local authorization server answers only what its server work has landed, so mock the rest from the contracts' example payloads; the other apps keep working because every new surface sits behind a token or an adapter member they do not advertise; Mark's global rules apply in that session as here.

- Mark's rules that bit this session: the compaction banner must be the first output after any compaction, before any tool call; no comments anywhere; reread before any edit, the file's modification time deciding; no shell that changes anything, and no PowerShell at all; the subagent is only for reads he asks for; anger is not an instruction.

## 7. Footnotes

- Google OAuth verification lists a terms and a privacy URL; after decision 8 the registered URLs become `/public/policies/terms` and `/public/policies/privacy`; Google sign-in is not in active use, so this is a one-time console edit whenever it comes up.
